# 実装指示書：パニック売り・底値圏検出機能の追加

## 0. この指示書について

本指示書は、既存の株式スクリーニング・アラートシステム（yfinance + pandas-ta + GitHub Actions +
LINE Messaging API、`要件定義書.md` / `CLAUDE.md` に基づき構築済み）に、市場全体のパニック売り
状態とその底値圏を検出する新機能を追加するための作業指示である。

Claude Code は本指示書に従い、**着手前に「1. 事前確認事項」を必ず実施**し、既存コードの実態と
本指示書の前提が食い違う場合は、実装を進める前にユーザーに確認すること。

---

## 1. 事前確認事項（着手前に必ず実施）

実装を始める前に、以下をリポジトリ内で確認し、前提が異なる場合は本指示書の該当箇所を調整すること。

1. `watchlist.csv` の実際の列構成を確認する。本機能は `ticker` 列（yfinance形式、例: `7203.T`,
   `AAPL`）の存在を前提とする。列名が異なる場合は、後述の `load_watchlist()` の
   `ticker_column` 引数を実際の列名に合わせて修正する。
2. 既存のLINE Messaging API送信処理（関数名・モジュール・認証情報の取得方法＝環境変数名など）を
   特定する。本機能内の `send_line_alert()` はプレースホルダーであり、既存の送信処理に置き換える。
3. 既存のRSI計算処理（`pandas-ta` の呼び出し方、Wilder方式14日設定）と重複しないか確認する。
   本機能は独自にRSIを計算するため、既存パイプラインで計算済みのRSIを再利用できる場合は
   二重計算を避けるよう統合する。
4. GitHub Actions の既存ワークフロー（`.github/workflows/*.yml`、1日2回・5:30 JST / 8:00 JST 実行）
   の構成を確認し、本機能をどちらの実行、または両方の実行に組み込むかを決定する
   （推奨: 市場全体の状態を見る性質上、2回とも実行し、直近の状態を常に反映させる）。

上記1〜4について、コードから自動判定できない、または複数の実装方針があり得る場合は、
実装を止めてユーザーに質問すること。

---

## 2. 追加するファイル

- `panic_bottom_detector.py`（提供済みモジュール）を、既存のディレクトリ構成に合わせて配置する。
  - 既存に `src/` や `detectors/` 等のディレクトリ構成があればそれに従う。
  - 特に構成が定まっていない場合は、リポジトリ直下、または `screener/` 等の既存スクリプト群と
    同じ階層に配置する。

このモジュールは以下の3指標のみで構成されている（会員限定データ・著作権保護された指数データの
スクレイピングは一切行わない設計）。

| 指標 | 内容 | データ源 |
|---|---|---|
| ①クロス市場デカップリング | watchlist平均騰落率が急落する一方、米国主要指数（S&P500・SOX）が反応していない状態 | yfinance |
| ②セリング・クライマックスbreadth | watchlist内で「出来高急増＋値幅拡大＋安値圏引け」に該当した銘柄の割合 | yfinance（既存OHLCV） |
| ③RSI極端乖離breadth＋連続急落 | watchlist内でRSI<20の銘柄の割合、および平均騰落率の連続急落 | 既存RSI計算ロジック |

3指標を重み付き合成した `market_panic_score` により `panic_alert`（パニック警戒）と
`bottom_candidate`（底打ち候補）の2種類のシグナルを出力する。

---

## 3. 依存関係

- 追加の外部ライブラリは不要（`yfinance`, `pandas`, `pandas-ta` は既存パイプラインで導入済みの前提）。
- 新規に会員登録・APIキー取得が必要な外部サービスは使用しない。

---

## 4. 統合手順

1. **ファイル配置**：`panic_bottom_detector.py` を配置する。
2. **`load_watchlist()` の調整**：事前確認事項1の結果に基づき、`ticker_column` 引数または
   実装を修正する。
3. **`send_line_alert()` の置き換え**：事前確認事項2で特定した既存のLINE送信関数を呼び出す
   実装に置き換える。メッセージ本文（パニック警戒／底打ち候補の2種）はそのまま利用してよいが、
   既存の通知フォーマット（絵文字・銘柄一覧の付記など）に合わせて調整して構わない。
4. **設定値（`PanicDetectorConfig`）の配置場所を決定**：既存の設定管理方式
   （環境変数／設定ファイル／コード直書き）に合わせて、`PanicDetectorConfig` の各閾値を
   どこで管理するか決定する。既存の設定方式が不明な場合はユーザーに確認する。
5. **実行フローへの組み込み**：`run_daily_check(watchlist_csv_path)` を、事前確認事項4で
   決定したタイミングでメインの実行スクリプトから呼び出す。既存のスクリーニング処理とは
   独立して実行できる（既存の個別銘柄アラートロジックとは影響し合わない設計）。
6. **エラーハンドリングの追加**：
   - `yfinance` のデータ取得失敗（一部銘柄・指数）が発生しても処理全体が止まらないよう、
     既存パイプラインのリトライ・ログ出力方針に合わせて実装する
     （`fetch_watchlist_data()` と `fetch_index_return_series()` は個別に例外を握りつぶす
     設計になっているため、失敗時のログ出力を追加することを推奨）。
   - watchlist内の有効データが極端に少ない場合（例：半数以上の銘柄でデータ取得失敗）は、
     `market_panic_score` の信頼性が低下する旨を通知メッセージに含めるか検討する。

---

## 5. テスト方針

1. **ユニットテスト**：`compute_market_panic_and_bottom()` に対して、パニック局面・平常局面・
   底打ち局面を模した合成データ（ダミーDataFrame）を入力し、期待通りのフラグが立つことを確認する。
2. **バックテスト**：2026年7月17日前後（半導体株急落、キオクシア急落）の実データを用いて、
   `market_panic_score` の推移と実際の値動きを突き合わせ、閾値（`volume_z_threshold`,
   `climax_breadth_threshold`, `rsi_oversold_breadth_threshold`, `alert_score_threshold` など）が
   妥当かを検証する。他の過去の急落局面（可能であれば複数）でも同様に検証することが望ましい。
3. バックテストの結果、閾値の調整が必要と判断される場合は、`PanicDetectorConfig` の
   デフォルト値を変更する前にユーザーに調整案を提示する。

---

## 6. 受け入れ基準

- [ ] `watchlist.csv` の実データで `run_daily_check()` がエラーなく完走する
- [ ] `market_panic_score`, `panic_alert`, `bottom_candidate` が日次で正しく計算される
- [ ] 既存のLINE通知経路で、パニック警戒／底打ち候補の2種のメッセージが正しく届く
- [ ] 一部銘柄・指数のデータ取得に失敗しても、処理全体が異常終了しない
- [ ] 既存の個別銘柄スクリーニング・アラート機能に影響を与えない（独立して動作する）

---

## 7. 未確定事項（ユーザーへの確認が必要）

- 各閾値（`volume_z_threshold` = 2.0、`climax_breadth_threshold` = 25%、
  `rsi_oversold_breadth_threshold` = 30%、`alert_score_threshold` = 4.0 など）は
  バックテストで検証されておらず、初期値のまま。運用しながら調整が必要。
- パニック警戒／底打ち候補の通知を、既存の個別銘柄アラートと同じLINEチャンネル・
  同じメッセージ形式で送るか、別チャンネル・別フォーマットにするか。
- `watchlist.csv` 全体を対象にするか、セクターごとに分けて複数の `market_panic_score` を
  算出するか（現状の実装は watchlist 全体の単一スコア）。

---

## 8. 参考：本機能が扱わない指標（意図的に除外）

以下は検討したが、著作権・利用規約上の制約、または会員限定データであるため、本機能には
含めていない。将来的に正規の有償データ契約（日経NEEDS、QUICK等）を利用する場合は
別途拡張を検討する。

- 信用評価損益率（日本経済新聞・松井証券等）
- 日経平均VI（日経平均プロフィル、知的財産権が明記されている）
